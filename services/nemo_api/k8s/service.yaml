apiVersion: v1
kind: Service
metadata:
  name: nemo-trainer
spec:
  selector:
    app: nemo-trainer
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  type: ClusterIP
